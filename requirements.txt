# Runtime dependencies, added incrementally as each phase needs them.
pandas==2.3.3
dvc[s3]==3.67.1
kaggle==1.8.4
scikit-learn==1.9.0
mlflow==3.14.0
PyYAML==6.0.3
python-dotenv==1.2.2

# Phase 4 will add: sagemaker, boto3
# Phase 6 will add: evidently
