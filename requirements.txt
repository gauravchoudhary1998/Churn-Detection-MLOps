# Runtime dependencies, added incrementally as each phase needs them.
pandas==2.3.3
dvc[s3]==3.67.1
kaggle==1.8.4

# Pinned to 1.4.2, NOT the latest scikit-learn: this is the newest version
# AWS's prebuilt SageMaker scikit-learn inference container supports
# (sagemaker-scikit-learn:1.4-2). A model pickled with a newer sklearn is not
# guaranteed to unpickle correctly in an older one, so training and serving
# must use the same version deliberately, not coincidentally.
scikit-learn==1.4.2
joblib==1.5.3
mlflow==3.14.0
PyYAML==6.0.3
python-dotenv==1.2.2
evidently==0.7.21
boto3==1.43.46
